FROM python:3.13-slim

# MLflow UI only; version aligned with pyproject.toml (MLflow 3.x)
RUN pip install --no-cache-dir "mlflow>=3.0,<4"

EXPOSE 5000

ENTRYPOINT ["mlflow", "ui", "--backend-store-uri", "file:///mlflow/mlruns", "--host", "0.0.0.0", "--port", "5000"]
